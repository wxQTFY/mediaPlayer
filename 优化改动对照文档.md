# mediaPlayer 安全优化改动对照

> 优化日期：2026-06-09
> 优化原则：安全优先、保留现有用户功能、尽量保持原有架构、所有关键改动可验证。

## 1. 优化结果概览

本轮优化保留了以下功能：

- 本地视频选择、播放、列表去重和持久化
- 网络视频播放与持久化
- HLS、DASH、FLV 播放
- 不兼容格式转 HLS 播放
- GPU 编码优先、CPU 编码回退
- 自定义窗口按钮、托盘和本地文件协议
- Windows、macOS、Linux 构建脚本

质量结果：

| 检查项 | 优化前 | 优化后 |
|---|---|---|
| `npm run typecheck` | 失败 | 通过 |
| `npm run build` | 失败 | 通过 |
| Lint 错误 | 64 个 | 0 个 |
| 自动化测试 | 无 | 4 项安全测试通过 |
| 生产依赖漏洞 | 8 个 | 最近一次成功审计为 0 个 |
| Electron | 39.7.0 | 39.8.10 |
| Vite | 7.3.1 | 7.3.5 |

说明：项目原有大量 Prettier 格式警告未进行全量自动格式化，避免产生大量与功能无关的代码差异。

## 2. 安全边界改动

### 2.1 本地媒体改为主进程授权访问

新增文件：

- `src/main/tools/mediaAccess.ts`

原实现：

```text
渲染层提供任意 realPath
→ Express 或 local-file 协议直接读取
```

优化后：

```text
用户通过系统文件选择框选择媒体
→ 主进程生成不可预测 UUID
→ 主进程登记 ID 与物理路径
→ 转码服务和 local-file 协议只接受已登记媒体
```

区别和原因：

- 原代码使用本地路径 MD5 作为 ID，知道路径即可推测缓存地址。
- 新代码使用随机 UUID，新媒体缓存地址不可预测。
- 未经文件选择或可信存储恢复的路径不能读取和转码。
- `store:set` 只能保存已授权本地媒体，不能通过伪造播放列表扩大文件权限。
- 网络 URL 仍允许保存，但只接受 `http:` 和 `https:`。

### 2.2 转码启动从公开 HTTP 接口迁移到受控 IPC

涉及文件：

- `src/main/controller/server/server.ts`
- `src/preload/index.ts`
- `src/preload/index.d.ts`
- `src/renderer/src/api/api.ts`
- `src/renderer/src/store/playerStory.ts`

原实现：

```ts
GET http://localhost:9999/video?realPath=任意路径&id=任意值&duration=任意值
```

优化后：

```ts
window.api.media.prepareStream(id, duration)
```

区别和原因：

- 移除了网页可以直接调用的 `/video` 转码接口。
- 渲染层不再向转码服务传递物理路径。
- 主进程通过授权 ID 查找真实路径。
- IPC 校验发送方必须是主窗口顶层 frame。
- 转码参数增加 ID、时长和授权状态校验。

用户功能不变：点击播放需要转码的视频时，仍会自动启动转码并播放。

### 2.3 HLS 文件服务严格限制访问范围

涉及文件：`src/main/controller/server/server.ts`

原实现：

```ts
app.use('/temp_hls', express.static(整个缓存目录))
```

优化后：

```text
只允许 /temp_hls/<已授权ID>/index.m3u8
只允许 /temp_hls/<已授权ID>/seg_<数字>.ts
```

区别和原因：

- 服务只监听 `127.0.0.1`。
- 使用随机空闲端口，避免固定 `9999` 端口冲突。
- 路由严格验证 ID 和文件名，禁止目录穿越与任意缓存文件读取。
- 播放器请求 HLS 文件时会刷新闲置计时器，避免正常播放过程中转码被提前停止。

### 2.4 Preload API 从通用 IPC 改为明确业务 API

涉及文件：

- `src/preload/index.ts`
- `src/preload/index.d.ts`
- `src/renderer/src/api/media.ts`
- `src/renderer/src/api/window.ts`
- `src/renderer/src/components/Versions.vue`

原实现：

```ts
contextBridge.exposeInMainWorld('electron', electronAPI)
window.electron.ipcRenderer.invoke(任意频道, 任意参数)
```

优化后：

```ts
window.api.window.minimize()
window.api.media.openFiles(videoList)
window.api.media.prepareStream(id, duration)
window.api.store.getVideoList()
window.api.store.setVideoList(videoList)
```

区别和原因：

- 渲染层不能再调用任意 IPC 频道。
- 每个公开方法都有明确参数和返回类型。
- 保留了窗口控制、文件选择、转码、存储和版本显示功能。

### 2.5 Electron 页面权限收紧

涉及文件：`src/main/index.ts`

原实现：

```ts
sandbox: false
shell.openExternal(details.url)
mainWindow.webContents.openDevTools()
```

优化后：

```ts
sandbox: true
只允许打开 http: 和 https: 外链
阻止主窗口跳转到其他页面
DevTools 仅开发环境开启
```

区别和原因：

- 开启渲染进程沙箱。
- 禁止危险协议传入系统外部处理器。
- 防止播放器页面被导航到不可信页面后继续使用 preload 能力。

### 2.6 自定义本地文件协议改为严格解析

涉及文件：`src/main/tools/localFileProtocol.ts`

原实现：

```ts
字符串替换协议头
→ decodeURIComponent
→ 读取任意指定路径
```

优化后：

```text
仅接受 GET
→ 使用 URL API 解析
→ 标准化 Windows / Unix 路径
→ 检查路径是否已由主进程授权
→ 读取文件
```

区别和原因：

- 修复 Unix 绝对路径开头斜杠被删除的问题。
- 畸形 URL 会在异常处理内返回错误。
- 未授权文件返回 `403`。

## 3. 转码稳定性改动

### 3.1 编码器探测不会永久挂起

涉及文件：`src/main/tools/encoderManage.ts`

原实现：

```ts
探测失败时直接 return
// Promise 永远不 resolve 或 reject
```

优化后：

```ts
探测失败时 resolve({ bestEncoder: 'libx264', isGpu: false })
```

效果：无法获取 GPU 编码器时自动使用 CPU，不会卡住播放流程。

### 3.2 按编码器使用兼容参数并自动回退

涉及文件：`src/main/controller/transCodeManage/transcode.ts`

原实现：

```text
所有编码器固定使用 -preset p1
```

优化后：

```text
h264_nvenc → -preset p1
libx264    → -preset veryfast
其他编码器 → 不强加不兼容 preset
GPU 实际启动失败 → 自动回退 libx264
```

效果：保留 GPU 加速，同时确保驱动或硬件编码不可用时仍能播放。

### 3.3 防止重复任务和失控 FFmpeg 进程

涉及文件：

- `src/main/controller/transCodeManage/transcode.ts`
- `src/main/controller/transCodeManage/transCodeManage.ts`
- `src/main/index.ts`

优化内容：

- 相同媒体的并发转码请求共享同一启动任务。
- 等待 HLS 索引超时时会终止 FFmpeg。
- 应用退出时停止全部转码任务。
- 停止当前任务时同步清理当前任务 ID 和计时器。

### 3.4 转码缓存迁移到系统临时目录

涉及文件：

- `src/main/config/config.ts`
- `.gitignore`

原实现：

```text
<当前工作目录>/temp_hls
```

优化后：

```text
<系统临时目录>/sanjin-mediaplayer/temp_hls
```

区别和原因：

- 打包应用不再依赖当前工作目录写权限。
- `temp_hls/` 已加入 Git 忽略规则。
- 不再建议把运行时视频切片提交到仓库。

## 4. 生命周期与功能稳定性改动

### 4.1 窗口重建不再重复创建托盘和监听器

涉及文件：

- `src/main/controller/InitManager/initManager.ts`
- `src/main/controller/Tray/trayClass.ts`
- `src/main/controller/Window/windowController.ts`

优化后：

- Store 和托盘只初始化一次。
- macOS 重建窗口时，托盘更新为新窗口引用。
- 窗口 IPC 监听器重新注册前先移除旧监听器。
- 窗口操作 IPC 校验发送来源。

### 4.2 本地媒体服务具备明确生命周期

涉及文件：

- `src/main/controller/server/server.ts`
- `src/main/index.ts`

优化后：

- Electron ready 后启动服务。
- 使用随机空闲端口。
- 应用退出前关闭服务。
- 服务启动失败会明确报错。

## 5. 类型、构建和代码质量改动

### 5.1 恢复类型检查和正式构建

涉及文件包括：

- `tsconfig.web.json`
- `src/common/types.ts`
- `src/main/tools/ffmpeg.ts`
- `src/renderer/src/axios/*`
- `src/renderer/src/components/Artplayer/playLibraries.ts`
- 部分 Vue 组件

主要区别：

- 删除无效的 TypeScript `ignoreDeprecations: "6.0"`。
- 补充媒体错误信息和 FFprobe 元数据类型。
- Axios 与播放器扩展从大量 `any` 改为明确类型或 `unknown`。
- 修复未使用导入、缺失返回类型和 Vue 模板解析错误。
- 保留原有页面交互和播放器能力。

### 5.2 electron-store 使用明确数据模型

涉及文件：`src/main/controller/electronStore/db.ts`

原实现：

```ts
const _Store = (Store as any).default || Store
```

优化后：

```ts
new Store<{ videoList: VideoItem[] }>({
  defaults: { videoList: [] }
})
```

区别和原因：移除动态 `any` 兼容写法，存储结构具备类型约束。

### 5.3 自动生成组件声明已同步

涉及文件：`src/renderer/components.d.ts`

正式构建重新生成了组件声明，移除了当前源码中已经不再使用的陈旧组件条目。该文件属于构建工具维护的类型声明。

## 6. 依赖安全改动

涉及文件：

- `package.json`
- `package-lock.json`

升级内容：

```text
Electron：39.7.0 → 39.8.10
Vite：7.3.1 → 7.3.5
```

新增：

```text
Vitest 3.2.6
```

生产依赖审计：

```text
优化前：8 个漏洞，其中 6 个高危、2 个中危
优化后：最近一次成功 npm audit --omit=dev 结果为 0 个漏洞
```

开发依赖仍可能报告来自构建工具链的间接漏洞，应在后续依赖升级周期继续处理，但不会被打入生产运行依赖。

## 7. 新增测试

新增文件：

- `tests/mediaAccess.test.ts`

测试内容：

1. 拒绝目录穿越和畸形媒体 ID。
2. 未授权任意路径不能访问。
3. 只有正确的 ID 与绝对路径组合可以获得授权。
4. 无效 ID 和相对路径授权请求会被忽略。

执行结果：

```text
Test Files  1 passed
Tests       4 passed
```

## 8. 本轮验证结果

```text
npm run typecheck       通过
npm run lint -- --quiet 通过，Lint 错误为 0
npm run test            通过，4/4
npm run build           通过
npm audit --omit=dev    最近一次成功结果为 0 个漏洞
git diff --check        通过
```

说明：

- 未执行真实桌面图形界面的人工播放测试，当前环境不适合完整验证窗口、托盘、GPU 编码和实际视频播放。
- npm 后续审计请求曾出现一次网络 TLS 连接失败，不影响此前已完成的成功审计结果。

## 9. 不属于本轮优化的已有改动

开始优化前，工作树中已存在以下用户改动，本轮未恢复或覆盖：

- `README.md` 已被修改。
- `temp_hls/6d58c4ba42ead4e7b5bc012773863834/` 下已跟踪缓存文件处于删除状态。

`原项目分析报告.md` 是上一轮分析阶段新增的项目评审文档。
